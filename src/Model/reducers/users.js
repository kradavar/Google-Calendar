import { SIGN_IN, SIGN_UP, SIGN_OUT } from "../actions/users";

import initialState from "../initialState/initialState";

export const users = (state = initialState.user, action) => {
  debugger;
  return state;
};
